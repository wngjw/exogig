import { IAnnotationMetadata, IDecorator, DecoratorType } from './MetadataFactory';
export interface IAnnotationMetadataHolder {
    [index: string]: IAnnotationMetadata;
}
export declare class MetadataHelper {
    static findAnnotationsMetaData(target: Object, annotation: IDecorator): Array<DecoratorType>;
    static findPropertyMetadata(target: Object, annotation: IDecorator): IAnnotationMetadataHolder;
    private static findMetadata(target, annotation, metadataName);
}
